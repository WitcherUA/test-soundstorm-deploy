name: Deploy test-soundstorm

on:
  workflow_dispatch:   # ручний запуск
  push:
    branches:
      - main           # деплой при пуші в main

jobs:
  deploy:
    runs-on: self-hosted   # використовує твій self-hosted runner

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Перевірка порту 80 і зупинка nginx
        run: |
          echo "🔍 Перевірка порту 80..."
          if ss -ltn | grep ':80 '; then
            echo "⚠️ Порт 80 зайнятий. Зупиняємо nginx..."
            sudo systemctl stop nginx || true
          fi

      - name: Завантаження образу
        run: |
          echo "📦 Завантаження образу..."
          sudo docker pull witcherua/test-soundstorm:

      - name: Видалення старого контейнера
        run: |
          echo "🧹 Видалення старого контейнера..."
          sudo docker stop test-soundstorm || true
          sudo docker rm test-soundstorm || true

      - name: Запуск нового контейнера
        run: |
          echo "🚀 Запуск нового контейнера..."
          sudo docker run -d \
            --name test-soundstorm \
            -v /etc/letsencrypt:/etc/letsencrypt:ro \
            -p 80:80 -p 443:443 \
            --restart unless-stopped \
            witcherua/test-soundstorm:

      - name: Перевірка статусу контейнера
        run: |
          echo "📊 Статус контейнера:"
          sudo docker ps -a | grep test-soundstorm
